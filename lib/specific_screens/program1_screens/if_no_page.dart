import 'package:flutter/material.dart';

class IfNoPage extends StatelessWidget {
  const IfNoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}